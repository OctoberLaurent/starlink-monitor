# Dépendances de développement / build.
-r requirements.txt
pillow>=10.0          # génération de l'icône
pyinstaller>=6.0      # packaging .app
pytest>=8.0           # tests unitaires
mypy>=1.10            # analyse statique des types (équivalent PHPStan)
ruff>=0.5             # linting
black>=24.0           # formatage